"""Core, headless building blocks: settings, dictation engine, sidecar, audio.

Nothing in this package imports the UI, so every module here can be exercised
from tests without a window, a microphone or a sidecar attached.
"""
